<div class="row">
  <div class="col-sm-4 col-md-4 col-lg-4"></div>
  <div class="col-sm-4 col-md-4 col-lg-4">
    <h2><%= t('.resend_unlock_instructions') %></h2>

    <%= bootstrap_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
        <%= devise_error_messages! %>

        <div class="field">
          <%= f.email_field :email, autofocus: true %>
        </div>

        <div class="actions">
          <%= f.submit t('.resend_unlock_instructions'), {class: 'btn btn-default btn-custom '} %>
        </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
  <div class="col-sm-4 col-md-4 col-lg-4"></div>
</div>